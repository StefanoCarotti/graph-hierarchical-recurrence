# ── Core: PyTorch & Graph Neural Networks ────────────────────────────────────
# Tested with torch 2.9.0 + PyG 2.7.0 on Python 3.11
torch>=2.0.0
torch_geometric>=2.4.0
# torch_scatter and torch_cluster must be installed from the PyG wheel index:
#   pip install torch_scatter torch_cluster \
#       -f https://data.pyg.org/whl/torch-<TORCH_VERSION>+<CUDA>.html
torch_scatter>=2.1.0
torch_cluster>=1.6.3

# ── Training framework ────────────────────────────────────────────────────────
lightning>=2.0.0
torchmetrics>=1.0.0

# ── Scientific computing ──────────────────────────────────────────────────────
numpy==1.26.4
scipy==1.11.4
pandas==2.1.4
networkx==3.5
scikit-learn==1.2.2

# ── Visualisation & utilities ─────────────────────────────────────────────────
matplotlib==3.8.0
seaborn==0.12.2
tqdm==4.65.0
pyyaml==6.0.1

# ── Hyperparameter search ─────────────────────────────────────────────────────
# Used by experiments/lrgb/search.py and benchmarks/echo
ray[tune]>=2.0.0
wandb>=0.15.0

# ── City network experiment ───────────────────────────────────────────────────
# Used by experiments/city_network/
h3>=3.7.6
osmnx==2.0.6
geopandas==1.1.1
shapely==2.1.2
contextily>=1.3.0
momepy==0.10.0
pyogrio==0.11.1
pyproj==3.7.2
rasterio>=1.3.0
fiona>=1.9.0

# ── LRGB benchmark ────────────────────────────────────────────────────────────
# Used by experiments/lrgb/
ogb>=1.3.6

# ── ECHO benchmark ───────────────────────────────────────────────────────────
# Used by benchmarks/echo/
torchdyn>=1.0.3
